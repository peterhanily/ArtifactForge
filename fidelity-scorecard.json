{
  "schema_version": "1.0",
  "generator": {
    "artifactforge_version": "0.0.1",
    "git_commit": "1beabd7",
    "sqlite_version": "3.50.4"
  },
  "gates": {
    "validity": {
      "verdict": "fail",
      "fails": [
        "hive: oracle 'libregf' is not installed \u2014 a missing oracle is a failure, not a skip",
        "prefetch: pyscca rejected it \u2014 OSError: pyscca_file_open: unable to open file. libscca_io_handle_read_volumes_information: invalid volume device path ",
        "pe: oracle 'lief' is not installed \u2014 a missing oracle is a failure, not a skip"
      ],
      "gaps": [
        "plist: plistlib both writes and reads the LaunchAgent plist, so it is not an independent oracle; a second reader is an open gap",
        "sqlite: sqlite3 both writes and reads these databases, so it is not an independent oracle; a second reader is an open gap"
      ],
      "oracle_reads_passed": 16,
      "oracle_reads_total": 24
    },
    "identity": {
      "verdict": "pass",
      "fails": [],
      "gaps": [
        "macOS scenes carry no binary and therefore no hash-shaped field; the keystone currently covers the Windows family only"
      ],
      "checks_total": 28,
      "checks_joined": 28
    },
    "inertness": {
      "verdict": "fail",
      "fails": [
        "prefetch: carries no in-band synthetic marker, so a copy that escapes its bundle cannot be recognised as generated",
        "sqlite: carries no in-band synthetic marker, so a copy that escapes its bundle cannot be recognised as generated",
        "plist: carries no in-band synthetic marker, so a copy that escapes its bundle cannot be recognised as generated"
      ],
      "gaps": [],
      "formats_marked": 6,
      "formats_total": 16
    },
    "solvability": {
      "verdict": "fail",
      "fails": [
        "the 'blind' adversary scores 100.0% against a 10% threshold \u2014 that many answers are obtainable without forensic work",
        "the 'listing' adversary scores 20.0% against a 10% threshold \u2014 that many answers are obtainable without forensic work",
        "no macos question requires joining two artifacts, so the benchmark cannot detect a broken cross-artifact pivot",
        "no windows question requires joining two artifacts, so the benchmark cannot detect a broken cross-artifact pivot"
      ],
      "gaps": [],
      "reference_solver_score": 1.0,
      "blind_solver_score": 1.0,
      "listing_solver_score": 0.2,
      "null_solver_score": 0.0,
      "constant_solver_score": 0.0,
      "join_questions_macos": 0,
      "join_questions_windows": 0
    }
  },
  "honest_gaps": [
    "Gate 1 (validity): plist: plistlib both writes and reads the LaunchAgent plist, so it is not an independent oracle; a second reader is an open gap",
    "Gate 1 (validity): sqlite: sqlite3 both writes and reads these databases, so it is not an independent oracle; a second reader is an open gap",
    "Gate 1 (validity) FAILING: hive: oracle 'libregf' is not installed \u2014 a missing oracle is a failure, not a skip",
    "Gate 1 (validity) FAILING: prefetch: pyscca rejected it \u2014 OSError: pyscca_file_open: unable to open file. libscca_io_handle_read_volumes_information: invalid volume device path ",
    "Gate 1 (validity) FAILING: pe: oracle 'lief' is not installed \u2014 a missing oracle is a failure, not a skip",
    "Gate 2 (identity): macOS scenes carry no binary and therefore no hash-shaped field; the keystone currently covers the Windows family only",
    "Gate 3 (inertness) FAILING: prefetch: carries no in-band synthetic marker, so a copy that escapes its bundle cannot be recognised as generated",
    "Gate 3 (inertness) FAILING: sqlite: carries no in-band synthetic marker, so a copy that escapes its bundle cannot be recognised as generated",
    "Gate 3 (inertness) FAILING: plist: carries no in-band synthetic marker, so a copy that escapes its bundle cannot be recognised as generated",
    "Gate 4 (solvability) FAILING: the 'blind' adversary scores 100.0% against a 10% threshold \u2014 that many answers are obtainable without forensic work",
    "Gate 4 (solvability) FAILING: the 'listing' adversary scores 20.0% against a 10% threshold \u2014 that many answers are obtainable without forensic work",
    "Gate 4 (solvability) FAILING: no macos question requires joining two artifacts, so the benchmark cannot detect a broken cross-artifact pivot",
    "Gate 4 (solvability) FAILING: no windows question requires joining two artifacts, so the benchmark cannot detect a broken cross-artifact pivot"
  ],
  "verdict": "gap"
}
